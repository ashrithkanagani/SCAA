AUDIT SESSION: rater_1 | domain=cloud | condition=explain_all
Number of decisions shown: 26 (of 26 total)
Start a timer now. Read the following, then answer the 5 questions below.
======================================================================

Job 0 (priority 2, tick 1): provisioning Server 0 instead of provisioning Server 1. Servers 0 and 1 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 0 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 1 (priority 1, tick 2): provisioning Server 1 instead of provisioning Server 2. Servers 1 and 2 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 1 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 2 (priority 1, tick 4): provisioning Server 0 instead of provisioning Server 1. Servers 0 and 1 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 0 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 3 (priority 1, tick 6): provisioning Server 1 instead of provisioning Server 2. Servers 1 and 2 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 1 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 4 (priority 3, tick 7): provisioning Server 0 instead of provisioning Server 2. Servers 0 and 2 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 0 was picked as the lower-indexed tied candidate. Flagged for audit because the commitment would be hard to undo (high irreversibility) and the job was high priority and/or close to its deadline (high impact).
Job 5 (priority 2, tick 9): provisioning Server 0 instead of provisioning Server 2. Servers 0 and 2 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 0 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 6 (priority 2, tick 10): provisioning Server 2 instead of provisioning Server 3. Servers 2 and 3 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 2 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 7 (priority 2, tick 11): provisioning Server 1 instead of provisioning Server 3. Servers 1 and 3 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 1 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 8 (priority 3, tick 16): provisioning Server 0 instead of provisioning Server 1. Servers 0 and 1 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 0 was picked as the lower-indexed tied candidate. Flagged for audit because the commitment would be hard to undo (high irreversibility) and the job was high priority and/or close to its deadline (high impact).
Job 9 (priority 1, tick 19): provisioning Server 1 instead of provisioning Server 2. Servers 1 and 2 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 1 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 10 (priority 2, tick 22): provisioning Server 0 instead of provisioning Server 1. Servers 0 and 1 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 0 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 11 (priority 2, tick 23): provisioning Server 1 instead of provisioning Server 2. Servers 1 and 2 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 1 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 12 (priority 3, tick 26): provisioning Server 0 instead of provisioning Server 2. Servers 0 and 2 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 0 was picked as the lower-indexed tied candidate. Flagged for audit because the commitment would be hard to undo (high irreversibility) and the job was high priority and/or close to its deadline (high impact).
Job 13 (priority 2, tick 27): provisioning Server 1 instead of provisioning Server 2. Servers 1 and 2 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 1 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 14 (priority 1, tick 31): provisioning Server 0 instead of provisioning Server 1. Servers 0 and 1 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 0 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 15 (priority 1, tick 32): provisioning Server 1 instead of provisioning Server 2. Servers 1 and 2 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 1 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 16 (priority 3, tick 34): provisioning Server 2 instead of provisioning Server 3. Servers 2 and 3 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 2 was picked as the lower-indexed tied candidate. Flagged for audit because the commitment would be hard to undo (high irreversibility) and the job was high priority and/or close to its deadline (high impact).
Job 17 (priority 1, tick 35): provisioning Server 1 instead of provisioning Server 3. Servers 1 and 3 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 1 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 18 (priority 3, tick 37): provisioning Server 0 instead of provisioning Server 2. Servers 0 and 2 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 0 was picked as the lower-indexed tied candidate. Flagged for audit because the commitment would be hard to undo (high irreversibility) and the job was high priority and/or close to its deadline (high impact).
Job 19 (priority 1, tick 38): provisioning Server 2 instead of provisioning Server 3. Servers 2 and 3 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 2 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 20 (priority 1, tick 39): provisioning Server 1 instead of provisioning Server 3. Servers 1 and 3 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 1 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 21 (priority 1, tick 42): provisioning Server 0 instead of provisioning Server 1. Servers 0 and 1 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 0 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 22 (priority 2, tick 44): provisioning Server 1 instead of provisioning Server 2. Servers 1 and 2 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 1 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 23 (priority 3, tick 46): provisioning Server 1 instead of provisioning Server 2. Servers 1 and 2 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 1 was picked as the lower-indexed tied candidate. Flagged for audit because the commitment would be hard to undo (high irreversibility) and the job was high priority and/or close to its deadline (high impact).
Job 24 (priority 2, tick 47): provisioning Server 0 instead of provisioning Server 2. Servers 0 and 2 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 0 was picked as the lower-indexed tied candidate. Flagged for audit because the commitment would be hard to undo (high irreversibility).
Job 25 (priority 3, tick 48): provisioning Server 2 instead of provisioning Server 3. Servers 2 and 3 were both free and scored identically for this task -- the scheduling policy does not distinguish between otherwise-idle servers -- so Server 2 was picked as the lower-indexed tied candidate. Flagged for audit because the commitment would be hard to undo (high irreversibility) and the job was high priority and/or close to its deadline (high impact).

======================================================================
QUIZ (answer briefly, then record your total time):

1. Why did the agent choose 'assign->M1' for task 17 (tick 35) over 'assign->M3'?
   Your answer: ______________________

2. Why did the agent choose 'assign->M2' for task 6 (tick 10) over 'assign->M3'?
   Your answer: ______________________

3. Why did the agent choose 'assign->M1' for task 23 (tick 46) over 'assign->M2'?
   Your answer: ______________________

4. Why did the agent choose 'assign->M0' for task 10 (tick 22) over 'assign->M1'?
   Your answer: ______________________

5. Why did the agent choose 'assign->M0' for task 2 (tick 4) over 'assign->M1'?
   Your answer: ______________________

Total time taken (mm:ss): ______
