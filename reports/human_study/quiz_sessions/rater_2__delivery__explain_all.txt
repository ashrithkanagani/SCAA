AUDIT SESSION: rater_2 | domain=delivery | condition=explain_all
Number of decisions shown: 24 (of 24 total)
Start a timer now. Read the following, then answer the 5 questions below.
======================================================================

Job 0 (priority 2, tick 1): dispatching Vehicle 0 instead of dispatching Vehicle 1. Vehicles 0 and 1 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 0 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 1 (priority 1, tick 2): dispatching Vehicle 1 instead of dispatching Vehicle 2. Vehicles 1 and 2 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 1 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 2 (priority 1, tick 4): dispatching Vehicle 2 instead of dispatching Vehicle 3. Vehicles 2 and 3 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 2 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 3 (priority 1, tick 6): dispatching Vehicle 0 instead of dispatching Vehicle 1. Vehicles 0 and 1 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 0 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 4 (priority 3, tick 7): dispatching Vehicle 1 instead of dispatching Vehicle 3. Vehicles 1 and 3 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 1 was picked as the lower-indexed tied candidate. Flagged for audit because the commitment would be hard to undo (high irreversibility) and the job was high priority and/or close to its deadline (high impact).
Job 5 (priority 2, tick 9): dispatching Vehicle 2 instead of dispatching Vehicle 3. Vehicles 2 and 3 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 2 was picked as the lower-indexed tied candidate. Flagged for audit because the commitment would be hard to undo (high irreversibility).
Job 6 (priority 2, tick 10): dispatching Vehicle 1 instead of dispatching Vehicle 3. Vehicles 1 and 3 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 1 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 7 (priority 2, tick 11): dispatching Vehicle 3 instead of dispatching Vehicle 4. Vehicles 3 and 4 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 3 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 8 (priority 3, tick 17): dispatching Vehicle 0 instead of dispatching Vehicle 1. Vehicles 0 and 1 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 0 was picked as the lower-indexed tied candidate. Flagged for audit because the commitment would be hard to undo (high irreversibility) and the job was high priority and/or close to its deadline (high impact).
Job 9 (priority 1, tick 20): dispatching Vehicle 1 instead of dispatching Vehicle 2. Vehicles 1 and 2 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 1 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 10 (priority 2, tick 23): dispatching Vehicle 0 instead of dispatching Vehicle 1. Vehicles 0 and 1 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 0 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 11 (priority 2, tick 24): dispatching Vehicle 1 instead of dispatching Vehicle 2. Vehicles 1 and 2 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 1 was picked as the lower-indexed tied candidate. Flagged for audit because the commitment would be hard to undo (high irreversibility).
Job 12 (priority 3, tick 27): dispatching Vehicle 2 instead of dispatching Vehicle 3. Vehicles 2 and 3 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 2 was picked as the lower-indexed tied candidate. Flagged for audit because the commitment would be hard to undo (high irreversibility) and the job was high priority and/or close to its deadline (high impact).
Job 13 (priority 2, tick 28): dispatching Vehicle 0 instead of dispatching Vehicle 3. Vehicles 0 and 3 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 0 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 14 (priority 1, tick 32): dispatching Vehicle 0 instead of dispatching Vehicle 1. Vehicles 0 and 1 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 0 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 15 (priority 1, tick 33): dispatching Vehicle 1 instead of dispatching Vehicle 2. Vehicles 1 and 2 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 1 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 16 (priority 2, tick 34): dispatching Vehicle 2 instead of dispatching Vehicle 3. Vehicles 2 and 3 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 2 was picked as the lower-indexed tied candidate. Flagged for audit because the commitment would be hard to undo (high irreversibility).
Job 17 (priority 1, tick 35): dispatching Vehicle 3 instead of dispatching Vehicle 4. Vehicles 3 and 4 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 3 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 18 (priority 1, tick 39): dispatching Vehicle 0 instead of dispatching Vehicle 1. Vehicles 0 and 1 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 0 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 19 (priority 1, tick 40): dispatching Vehicle 1 instead of dispatching Vehicle 2. Vehicles 1 and 2 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 1 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 20 (priority 2, tick 44): dispatching Vehicle 1 instead of dispatching Vehicle 2. Vehicles 1 and 2 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 1 was picked as the lower-indexed tied candidate. Flagged for audit because the commitment would be hard to undo (high irreversibility).
Job 21 (priority 3, tick 48): dispatching Vehicle 0 instead of dispatching Vehicle 2. Vehicles 0 and 2 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 0 was picked as the lower-indexed tied candidate. Flagged for audit because the commitment would be hard to undo (high irreversibility) and the job was high priority and/or close to its deadline (high impact).
Job 22 (priority 2, tick 50): dispatching Vehicle 1 instead of dispatching Vehicle 2. Vehicles 1 and 2 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 1 was picked as the lower-indexed tied candidate. (Not flagged by SCAA's severity gate -- shown here only because this condition displays every decision.)
Job 23 (priority 3, tick 54): dispatching Vehicle 0 instead of dispatching Vehicle 1. Vehicles 0 and 1 were both free and scored identically for this order -- the dispatch policy does not distinguish between otherwise-idle vehicles -- so Vehicle 0 was picked as the lower-indexed tied candidate. Flagged for audit because the commitment would be hard to undo (high irreversibility) and the job was high priority and/or close to its deadline (high impact).

======================================================================
QUIZ (answer briefly, then record your total time):

1. Why did the agent choose 'assign->M1' for order 4 (tick 7) over 'assign->M3'?
   Your answer: ______________________

2. Why did the agent choose 'assign->M0' for order 18 (tick 39) over 'assign->M1'?
   Your answer: ______________________

3. Why did the agent choose 'assign->M0' for order 10 (tick 23) over 'assign->M1'?
   Your answer: ______________________

4. Why did the agent choose 'assign->M0' for order 3 (tick 6) over 'assign->M1'?
   Your answer: ______________________

5. Why did the agent choose 'assign->M1' for order 15 (tick 33) over 'assign->M2'?
   Your answer: ______________________

Total time taken (mm:ss): ______
